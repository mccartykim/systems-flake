# {{HOSTNAME}} NixOS configuration
# Generated by flake-installer on {{DATE}}
{
  config,
  pkgs,
  lib,
  ...
}: {
  imports = [
    ./hardware-configuration.nix
    ./disko.nix
{{PROFILE_IMPORTS}}
  ];

  # Hostname
  networking.hostName = "{{HOSTNAME}}";

  # System version - update this when doing major upgrades
  system.stateVersion = "{{STATE_VERSION}}";

{{EXTRA_CONFIG}}
}
