result
.DS_Store

# Age/Agenix - these are safe to commit
# *.age files are encrypted and safe
# But NEVER commit these:
secrets/age-key.txt
secrets/.age-recipients.backup
*.key
*.crt
.esphome/

# Beads runtime state
.beads/last-touched

# Claude Code local settings
.claude/settings.local.json
